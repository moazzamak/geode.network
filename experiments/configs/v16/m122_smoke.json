{
  "_smoke_note": "NOT A RESULT AND NEVER QUOTABLE AS ONE. Single atom count (128), tiny n ladder (2000/4000), tiny hash pool (20000 patches), gates bypassed. Its only job is to prove the code paths execute end-to-end: whitener+dictionary, ITQ/random hash fit at 108 AND 216 bits (two-projection concatenation), per-arm train/test binary code memmaps (GPU sign-GEMM), ridge fit + full-test score at every cell, gate bypass, evidence write. Every accuracy it produces is meaningless.",
  "_smoke_skip_gates": true,
  "corpus": {
    "dataset": "DomainNet, six domains, 345 classes",
    "source": "GEODE_CACHE_DIR/domainnet/repository/data (parquet)",
    "image_size": 32,
    "train_rows_per_class": 400,
    "test_rows_per_class": 100,
    "subsample_seed": 107,
    "shuffle_seed": 11,
    "pixel_identity_rows": 64,
    "expected_subsample_sha256": "63f590097008f749f3f1828b29d6f154de7b21a6828a7b017ac141c0615fa09d"
  },
  "device": {
    "whiten": "cpu",
    "hash": "cpu",
    "encode": "gpu",
    "ridge": "cpu"
  },
  "parity_guard": {
    "bound_relative": 1e-04,
    "models": ["small"],
    "resolution": 28,
    "batch": 4,
    "feature": "CLS token concatenated with the mean of the patch tokens"
  },
  "sparse": {
    "atoms_ladder": [128],
    "bits_ladder": [108, 216],
    "patch": 6,
    "stride": 1,
    "pool_grid": 2,
    "contrast_epsilon": 10.0,
    "zca_epsilon": 0.1,
    "zca_fit_patches": 400000,
    "zca_fit_seed": 11,
    "whitener": "global M108",
    "dictionary_rule": "prefix of the seeded permutation of the shared pool",
    "dictionary_seed": 11,
    "candidate_pool_size": 8192,
    "hash_fit_seed": 33,
    "hash_fit_images": 60,
    "hash_pool_size": 20000,
    "hash_iters": 10,
    "arms": ["b_random", "c_itq"],
    "bits_construction": "216 = two independent 108-bit projections concatenated (seeds 33 and 34)"
  },
  "head": {
    "family": "closed_form_ridge",
    "penalties": [1.0]
  },
  "scaling": {
    "n_ladder": [2000, 4000],
    "subsample_rule": "first n rows of the shuffled train order",
    "test_set": "full 34500-row shared test set"
  },
  "gate": {
    "t1_tolerance": 0.002,
    "kill_switch_1_joint_buyback": "gap(6144,138000,216) <= gap(3072,138000,108) - 0.01 (per arm)"
  },
  "numerics": {
    "encode_dtype": "float32",
    "gram_dtype": "float64",
    "torch_threads": 16,
    "seed": 109
  }
}
