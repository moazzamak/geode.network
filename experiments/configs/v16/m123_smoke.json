{
  "_smoke_note": "NOT A RESULT AND NEVER QUOTABLE AS ONE. Uses only the first 8000 train rows for the Gram (a different object than the sealed 138000-row spectrum) and the gates are bypassed. Its only job is to prove the code paths execute: memmap load, streaming Gram + class sums, standardisation, eigh, kappa fixed points, per-class S/Sigma assembly, Gaussian Monte-Carlo margin model for both families, crossing detection, margin diagnostics, evidence write. Every number it produces is meaningless.",
  "_smoke_skip_gates": true,
  "_smoke_rows": 8000,
  "milestone": "M123",
  "registered_in": "analysis/RESEARCH_IMPLEMENTATION_PLAN_v19.md section 5.2 (M123, smoke)",
  "source": {
    "features": "data_cache_root()/v16/m116/f_train.npy and dense_train.npy (first _smoke_rows rows only)"
  },
  "theory": {
    "formula": "per-class modal margin model (see sealed config)",
    "lambda_grid": [1.0],
    "ladder": [2000, 4000, 8000],
    "n_mc_samples": 2000,
    "mc_seed": 109,
    "disclosure": "smoke only"
  },
  "gate": {
    "predicted_crossing": "within [1/3, 3] of the measured M116 crossing midpoint (bypassed in smoke)",
    "gap_direction_at_nmax": "reported"
  },
  "numerics": {
    "gram_dtype": "float64",
    "eigh_backend": "numpy.linalg.eigh",
    "kappa_fixed_point_iters": 200,
    "kappa_fixed_point_tol": 1e-12,
    "torch_threads": 16,
    "seed": 109
  }
}
