{
  "milestone": "M167",
  "title": "Behavioral-transfer label protocol v0: cross-domain fit vs label-permuted control",
  "registered_in": "analysis/RESEARCH_IMPLEMENTATION_PLAN_v24.md section 7 Phase A (M167) / section 12 dispatch entry (17 Aug 2026)",
  "registered_before_any_label": true,
  "question": "Does the harness produce transfer labels that order known-similar above known-dissimilar on the measured axes?",
  "gate": {
    "registered": "two-tier (the M167a repair): validity = own-domain - permuted >= 0.05 (the instrument orders similarity at all); transfer = cross-domain - permuted >= 0.0 (the cross fit beats the permutation control); the harness is VOID for label production if validity fails",
    "validity_margin": 0.05,
    "transfer_margin": 0.0,
    "similar": "fit the SPM codes on domain-0 train rows, score on domain-1 test rows (same 345 classes, different domains)",
    "dissimilar": "the SAME fit on label-permuted domain-0 rows (permutation within the selected rows destroys class structure; the read must collapse toward 1/345)",
    "own_domain": "fit domain-0 train rows, score domain-0 test rows (the similar anchor)",
    "adjacent_domains_note": "strong cross-domain labels need adjacent domain pairs; deferred until the domain-name->index mapping is pinned"
  },
  "corpus": {
    "dataset": "DomainNet, six domains, 345 classes",
    "source": "GEODE_CACHE_DIR/domainnet/repository/data (parquet)",
    "image_size": 32,
    "train_rows_per_class": 400,
    "test_rows_per_class": 100,
    "subsample_seed": 107,
    "shuffle_seed": 11,
    "pixel_identity_rows": 64,
    "expected_subsample_sha256": "63f590097008f749f3f1828b29d6f154de7b21a6828a7b017ac141c0615fa09d"
  },
  "artifacts": {
    "cache_relpath": "v16/m142_c2",
    "spm_train_file": "spm1923_fulltrain.npy",
    "spm_test_file": "spm1923_fulltest.npy",
    "labels_file": "m142_c2_fulltrain_labels.npz"
  },
  "sparse": {
    "power": 0.5,
    "penalty": 1.0
  },
  "pairs": {
    "domain_a": 0,
    "domain_b": 1,
    "permutation_seed": 7,
    "note": "two distinct domains, same 345 classes; the permuted control reuses the SAME selected rows with labels shuffled inside the selection"
  },
  "numerics": {
    "block": 4096,
    "torch_threads": 16
  },
  "output_dir": "logs/results/v24/m167_transfer_labels"
}
