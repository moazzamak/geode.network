{
  "milestone": "M237",
  "title": "Quickdraw MLP probe on the concatenated CLIP + dino-b features",
  "question": "Does a one-hidden-layer probe on the strongest two backbones close the quickdraw bar (>= 0.8)?",
  "registered_in": "analysis/RESEARCH_IMPLEMENTATION_PLAN_v25.md M237 REGISTERED (20 Aug)",
  "corpus": {
    "dataset": "DomainNet, six domains, 345 classes",
    "image_size": 32,
    "train_rows_per_class": 400,
    "test_rows_per_class": 100,
    "subsample_seed": 107,
    "expected_subsample_sha256": "63f590097008f749f3f1828b29d6f154de7b21a6828a7b017ac141c0615fa09d"
  },
  "features": {
    "clip_train": "logs/results/v25/m236_clip_vitl14/features/clip_train.npy",
    "clip_test": "logs/results/v25/m236_clip_vitl14/features/clip_test.npy",
    "dino_train": "logs/results/v25/m234_vitb14/features/vitb14_train_dino.npy",
    "dino_test": "logs/results/v25/m234_vitb14/features/vitb14_test_dino.npy"
  },
  "artifacts": {"labels_file": "v16/m142_c2/m142_c2_fulltrain_labels.npz"},
  "training": {"epochs": 30, "lr": 0.001, "weight_decay": 0.0001,
               "batch": 1024, "seed": 11, "hidden": 512,
               "note": "fixed schedule, no test tuning"},
  "domain_names": ["clipart", "infograph", "painting", "quickdraw", "real", "sketch"],
  "ladder": {"easy": ["quickdraw", "real", "clipart"], "easy_bar": 0.8,
             "middle": ["sketch", "painting"], "middle_bar": [0.5, 0.6],
             "hard": ["infograph"], "hard_first_bar": [0.3, 0.4]},
  "interpretation_registered_before_running": {
    "g2_fail": "VOID - the schedule permutation must reconstruct the labels byte-exactly",
    "g3_fail": "VOID - the probe must be reproducible under the same seed",
    "reading": "quickdraw >= 0.8 closes the ladder; otherwise the quickdraw wall is DECLARED and the ladder closes at 5/6 easy bars with a registered quickdraw-specific representation cell"
  }
}
