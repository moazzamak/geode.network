[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: single-concern, in-process tests (no cross-module flows)
    integration: multi-module in-process flows
    system: cross-process tests (EVM harness, external tools)
addopts = -ra
