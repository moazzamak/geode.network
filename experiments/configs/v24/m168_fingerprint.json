{
  "milestone": "M168",
  "title": "Fingerprint embedder build: additive attribute embeddings + tiny MLP (+ continuity interface)",
  "registered_in": "analysis/RESEARCH_IMPLEMENTATION_PLAN_v24.md section 7 Phase B (M168) / section 12 entry (17 Aug 2026)",
  "registered_before_any_training": true,
  "model": {
    "f_dim": 16,
    "mlp_hidden": 32,
    "mlp_on": true,
    "seed": 11,
    "continuity_bins": null,
    "note": "f = normalise(sum_k emb(token_k) + mlp(token one-hot)); the continuity channel (sweep embedding) is reserved for M169 training"
  },
  "invariants": {
    "I1": "frozen at inference; deterministic (fixed seeds, no dropout)",
    "I2": "additive attribute-embedding sum; unit-normalised output"
  },
  "gates_built_now": {
    "G1_determinism": "covered by test_v24_m168_fingerprint (same encoder re-run identical; fresh encoder with the same seed identical)"
  },
  "output_dir": null
}
