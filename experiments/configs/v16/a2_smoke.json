{
  "milestone": "A2-smoke",
  "title": "A2 instrument smoke test — INADMISSIBLE AS EVIDENCE",
  "registered_in": "analysis/RESEARCH_IMPLEMENTATION_PLAN_v17.md section 3 (A2)",
  "_smoke_note": "NOT A RESULT AND NEVER QUOTABLE AS ONE. Eight rows per class, atoms 128, resolutions {42,224}, head grid {1,2} epochs x {1e-4,1e-3} lr. Its only job is to prove the code paths execute: parity guard, corpus, whitener/dictionary, frozen sparse-code and dense-feature encoding, ridge reference, SGD head grid with early stopping, reproduction-gate bypass, and the kill switch evaluates. Every accuracy it produces is meaningless.",
  "_smoke_skip_gates": true,
  "corpus": {
    "dataset": "DomainNet, six domains, 345 classes",
    "source": "GEODE_CACHE_DIR/domainnet/repository/data (parquet)",
    "image_size": 32,
    "train_rows_per_class": 8,
    "test_rows_per_class": 2,
    "subsample_seed": 107,
    "shuffle_seed": 11,
    "pixel_identity_rows": 16,
    "expected_subsample_sha256": null
  },
  "device": {
    "encode": "cuda",
    "whiten": "cpu",
    "training": "cuda",
    "ridge": "cpu"
  },
  "parity_guard": {
    "bound_relative": 1e-04,
    "models": ["small"],
    "resolution": 28,
    "batch": 2,
    "feature": "CLS token concatenated with the mean of the patch tokens"
  },
  "dense": {
    "model": "dinov2-small",
    "resolutions": [42, 224],
    "pixels": "original",
    "feature": "CLS token concatenated with the mean of the patch tokens"
  },
  "sparse": {
    "atoms": 128,
    "arm": "a_random",
    "pool_grid": 2,
    "patch": 6,
    "stride": 1,
    "contrast_epsilon": 10.0,
    "zca_epsilon": 0.1,
    "image_size": 32
  },
  "ridge": {
    "penalty": 1.0
  },
  "schedule": {
    "optimiser": "AdamW",
    "batch": 16,
    "head_grid": {
      "epochs": [1, 2],
      "learning_rate": [1e-4, 1e-3]
    },
    "lr_schedule": "cosine",
    "weight_decay": 1e-4,
    "early_stopping": {"metric": "validation_accuracy", "patience": 2, "min_epochs": 1},
    "validation_fraction": 0.05
  },
  "gate": {
    "registered_prediction": "SMOKE ONLY — no prediction is read from a smoke run.",
    "t1_reproduction_tolerance": 0.002,
    "t2_reproduction_tolerance": 0.01,
    "kill_switch": "SMOKE ONLY."
  },
  "numerics": {
    "encode_dtype": "float32",
    "gram_dtype": "float64",
    "torch_threads": 16,
    "seed": 109
  }
}
